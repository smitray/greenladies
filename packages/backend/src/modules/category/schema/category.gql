type Category implements Node {
	id: ID!
	name: String!
	isActive: Boolean!
	# position: Int!
	# level: Int!
	# path: String!
	# availableSortBy: [String!]!
	includeInMenu: Boolean!
	children: [Category!]!
	parent: Category
}
